version: '3.8'

services:
  face-mask-app:
    build:
      context: .
      dockerfile: Dockerfile.mask
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
